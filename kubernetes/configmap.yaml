apiVersion: v1
kind: ConfigMap
metadata:
  name: llm-config
  namespace: llm-system
data:
  redis-host: "redis-service"
  redis-port: "6379"
  log-level: "INFO"
  rate-limit-per-minute: "60"
  max-input-length: "4096"
